fullName:
  name: network-policy
  workspaceName: network-workspace
spec:
  input:
    rules:
    - ruleSpec:
      - podSelector:
        - key: tier
          value: app
    toPodLabels: []
  recipe: custom-ingress
  recipeVersion: v1
  type: network-policy